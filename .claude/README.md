# `.claude/` — Claude Code

| Folder / file | Description |
|---------------|-------------|
| `settings.json` | Hooks (see `hooks/*.sh`) |
| `hooks/` | `block-sensitive-files.sh`, `run-unit-tests.sh` |
| `skills/` | `code-companion-conventions`, `release-desktop` |
| `agents/` | `security-pass`, `mcp-contract-check` |
| `commands/` | Slash commands (e.g. `validate-project`) |

See **`docs/CLAUDE-CODE-AUTOMATION.md`** for setup, MCP, and prerequisites (**bash**, **jq**).
