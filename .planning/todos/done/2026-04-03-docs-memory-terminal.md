# Done — Docs & tasks (2026-04-03)

## Shipped in code (reference)

- Memory: `lib/memory.js`, `server.js` (`/api/chat`), `src/App.jsx` (`conversationId`, `saveConversation`).
- TERMINALFIX: `lib/builtin-agent-tools.js`, `lib/tool-call-handler.js`.
- E2E: `tests/e2e/image-upload.spec.js` (duplicate upload dialog).

## Documentation updated

- `docs/ENVIRONMENT_VARIABLES.md` — `memory` config row; section on memory + chat scoping.
- `docs/TESTING.md` — unit test count (148), `memory-scope` / tool-call tests called out.
- `CLAUDE.md` — Settings → Memory tab (per-conversation retrieval).
- `CHANGELOG.md` — [Unreleased] Changed/Fixed entries for memory, terminal prompt, E2E.
- `.planning/STATE.md`, `whats-next.md` — Task 14 narrative.

## Tasks

- Archon: task **Docs & planning: memory scope, TERMINALFIX, testing (2026-04-03)** → `done` (project Code Companion).
