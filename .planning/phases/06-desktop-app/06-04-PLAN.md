---
phase: 06-desktop-app
plan: 04
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
files_modified: []
autonomous: false
requirements:
  - DESK-01
  - DESK-02
  - DESK-03
  - DESK-04
  - DESK-05

must_haves:
  truths:
    - "App launches as a native desktop window without manual terminal commands"
    - "Express server starts on a free port automatically"
    - "All 8 modes work inside the Electron window"
    - "Settings panel shows data management controls"
    - "Ollama connection dot reflects real status"
---

<objective>
Build the Vite production assets and verify the complete Electron desktop app works end-to-end: native window launch, Express server boot, all modes functional, data persistence, and connection status.

Purpose: This is the integration verification plan that confirms all three prior plans work together as a complete desktop application before attempting installer builds.
Output: Verified working Electron app ready for distribution.
</objective>

<execution_context>
@/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-desktop-app/06-CONTEXT.md
@.planning/phases/06-desktop-app/06-01-SUMMARY.md
@.planning/phases/06-desktop-app/06-02-SUMMARY.md
@.planning/phases/06-desktop-app/06-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build production assets and smoke-test Electron launch</name>
  <files></files>
  <action>
1. Run `npm run build` to create Vite production build in `dist/`
2. Run `npx electron electron/main.js` to launch the Electron app in dev mode
3. Verify in the terminal output:
   - Express starts on a dynamically detected port (not hardcoded 3000)
   - No EADDRINUSE errors
   - No module resolution errors
   - Window opens and loads the app
4. If any errors occur, fix them:
   - Common issues: missing require paths after data dir changes, ASAR-related path issues (shouldn't appear in dev mode but check)
   - Verify server.js respects CC_DATA_DIR for config/history/logs
5. Kill the Electron process after confirming it boots successfully
  </action>
  <verify>
    <automated>cd /Users/james/AI_Dev/AIApp-CodeCompanion && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Vite build succeeds. Electron app launches without errors in dev mode.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Electron desktop app</name>
  <files></files>
  <action>
Present the complete Electron desktop application for user verification. The app should be launchable via `npm run electron:dev` and demonstrate all desktop-specific features built across plans 01-03.
  </action>
  <verify>User confirms all 10 verification steps pass</verify>
  <done>User types "approved" confirming the desktop app works correctly</done>
  <what-built>Complete Electron desktop application with embedded Express server, data management, Ollama setup wizard, cross-platform IDE launchers, and auto-update configuration.</what-built>
  <how-to-verify>
    1. Run `npm run electron:dev` from the project root
    2. Verify the app opens in a native macOS window (not a browser tab)
    3. Verify it starts maximized (or remembered size from last session)
    4. Verify the menu bar shows Edit, View, Window, Help (no File menu)
    5. Check the model dropdown area for a green/red connection dot
    6. Switch between modes (Chat, Review, etc.) — all should work
    7. Open Settings — verify "Data Management" section with Export/Import buttons
    8. Close and reopen the app — verify it restores the last active mode
    9. Close and reopen again — verify window size/position is remembered
    10. If Ollama is not running: verify you see the friendly setup wizard (not an error)
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the desktop app works, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All five DESK requirements verified:
- DESK-01: App launches as native desktop window
- DESK-02: Express server auto-starts on free port
- DESK-03: IDE launcher code inspected for cross-platform commands
- DESK-04: electron-builder config exists for all three platform targets
- DESK-05: Data stored in CodeCompanion-Data directory
</verification>

<success_criteria>
- User confirms the Electron app launches and works correctly
- All 8 modes functional inside the desktop window
- No manual terminal commands required to use the app
- Settings panel shows Electron-specific data management controls
</success_criteria>

<output>
After completion, create `.planning/phases/06-desktop-app/06-04-SUMMARY.md`
</output>
