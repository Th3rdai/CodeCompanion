---
phase: 28-multi-file-code-review
plan: "00"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/review-files.test.js
  - tests/integration/review-folder.test.js
autonomous: true
requirements:
  - MREV-01

must_haves:
  truths:
    - "Test files exist and pass (as skips) before any implementation begins"
    - "Unit test stubs cover reviewFiles() combined-string format and timeout scaling"
    - "Integration test stubs cover /api/review/folder/preview and /api/review/folder shapes"
  artifacts:
    - path: "tests/unit/review-files.test.js"
      provides: "Unit test stubs for reviewFiles() — skip until Wave 1 implements the function"
      min_lines: 30
    - path: "tests/integration/review-folder.test.js"
      provides: "Integration test stubs for the two new folder review routes — skip until Wave 1 implements the routes"
      min_lines: 30
  key_links:
    - from: "tests/unit/review-files.test.js"
      to: "lib/review.js"
      via: "require('../../lib/review')"
      pattern: "require.*lib/review"
    - from: "tests/integration/review-folder.test.js"
      to: "/api/review/folder"
      via: "HTTP POST"
      pattern: "api/review/folder"
---

<objective>
Create Wave 0 test scaffold files so every subsequent task has an automated verify command. Tests are written as skips (test.skip / it.skip) and must pass in the current codebase with no implementation yet in place.

Purpose: Nyquist compliance — no task proceeds without a green automated verify command.
Output: Two test files with skipped stubs; `npm test` passes before any implementation.
</objective>

<execution_context>
@/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-multi-file-code-review/28-CONTEXT.md
@.planning/phases/28-multi-file-code-review/28-RESEARCH.md
@.planning/phases/28-multi-file-code-review/28-VALIDATION.md

<interfaces>
<!-- Existing test patterns for this project — use node:test (not Jest). -->

From tests/unit/pentest-orchestration.test.js:
```javascript
const { describe, it } = require("node:test");
const assert = require("node:assert/strict");

describe("pentestCode export", () => {
  it("pentestCode is a function", () => {
    const { pentestCode } = require("../../lib/pentest");
    assert.equal(typeof pentestCode, "function");
  });
});
```

From tests/integration/api-with-images.test.js (server-spawn pattern):
```javascript
const test = require("node:test");
const { spawn } = require("child_process");
async function waitForServer(baseUrl, timeoutMs = 15000) { /* poll /api/config */ }
```

From lib/review.js (current exports):
```javascript
module.exports = { reviewCode, getTimeoutForModel };
// NOTE: reviewFiles does NOT exist yet — stubs must use test.skip
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit test stubs for reviewFiles()</name>
  <files>tests/unit/review-files.test.js</files>
  <behavior>
    - test.skip: reviewFiles is exported from lib/review (currently absent — skip until Wave 1)
    - test.skip: reviewFiles builds combined string with "// --- FILE: path ---" separators
    - test.skip: reviewFiles scales timeout by file count using Math.ceil(count/5) formula, capped at 600000ms
    - test.skip: reviewFiles timeout never exceeds 600000ms for large file counts (e.g. 80 files)
    - test.skip: reviewFiles passes combined string to reviewCode internally (pure unit — check return type)
  </behavior>
  <action>
    Create tests/unit/review-files.test.js using node:test with test.skip for all cases. Use describe("reviewFiles", ...) wrapper. All tests must use test.skip so they pass immediately (before implementation). Do NOT use test() — use the describe/it pattern from pentest-orchestration.test.js.

    The file should require("../../lib/review") at the top of each describe block. Since reviewFiles doesn't exist yet, wrap requires inside the skip callbacks so the file loads cleanly.

    Include stubs for:
    1. Export check — `typeof reviewFiles === "function"`
    2. Combined string format — given files [{path:"a.js",content:"x"},{path:"b.js",content:"y"}] the combined string contains "// --- FILE: a.js ---" and "// --- FILE: b.js ---"
    3. Timeout scaling — 10 files with base 300000ms timeout → scaled timeout = Math.min(300000 * Math.ceil(10/5), 600000) = 600000
    4. Timeout cap — 80 files should not exceed 600000ms
    5. Return type — returns a Promise (async function)
  </action>
  <verify>
    <automated>node --test tests/unit/review-files.test.js</automated>
  </verify>
  <done>tests/unit/review-files.test.js exists, all tests pass (as skips), `node --test tests/unit/review-files.test.js` exits 0</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create integration test stubs for folder review routes</name>
  <files>tests/integration/review-folder.test.js</files>
  <behavior>
    - test.skip: POST /api/review/folder/preview with {folder: "/tmp"} returns JSON with files array, totalSize, skipped
    - test.skip: POST /api/review/folder/preview with missing folder returns 400
    - test.skip: POST /api/review/folder with {model: "llama3.2", folder: "/tmp/test-project"} returns JSON with type "report-card" or SSE fallback stream
    - test.skip: POST /api/review/folder with missing folder returns 400
  </behavior>
  <action>
    Create tests/integration/review-folder.test.js using node:test. Use test.skip for all tests. Pattern from tests/integration/api-with-images.test.js — spawn the server, wait for it, make HTTP calls.

    Since these are skips, the server spawn and fetch calls go inside the skip callbacks. The file should load cleanly without starting a real server.

    Include stubs for:
    1. Preview endpoint happy path — response shape: { files: [{path, size}], totalSize, skipped, folder }
    2. Preview endpoint missing folder — response: 400 { error: "Missing folder" }
    3. Folder review happy path — response has type: "report-card" with data.overallGrade
    4. Folder review missing fields — response: 400 { error: "Missing model or folder" }

    Use a port not conflicting with other integration tests (use 3325 for these tests).
  </action>
  <verify>
    <automated>node --test tests/integration/review-folder.test.js</automated>
  </verify>
  <done>tests/integration/review-folder.test.js exists, all tests pass (as skips), `node --test tests/integration/review-folder.test.js` exits 0</done>
</task>

</tasks>

<verification>
`npm test` passes — all existing unit tests still green, two new test files pass with skips.

```bash
npm test
node --test tests/unit/review-files.test.js
node --test tests/integration/review-folder.test.js
```
</verification>

<success_criteria>
- tests/unit/review-files.test.js exists with 5 skipped test stubs covering reviewFiles() behavior
- tests/integration/review-folder.test.js exists with 4 skipped test stubs covering route shapes
- `npm test` exits 0 (no regressions)
- Both new files exit 0 (all tests skip, none fail)
</success_criteria>

<output>
After completion, create `.planning/phases/28-multi-file-code-review/28-00-SUMMARY.md`
</output>
