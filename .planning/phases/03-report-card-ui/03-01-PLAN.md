---
phase: 03-report-card-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/LoadingAnimation.jsx
  - src/components/ReportCard.jsx
  - tests/ui/loading-animation.spec.js
  - tests/ui/report-card-interactions.spec.js
autonomous: true
requirements: [REVW-07, REVW-08]

must_haves:
  truths:
    - "User sees playful loading animation with encouraging messages while review processes"
    - "User sees minimal report card by default (grades + top priority)"
    - "User can expand to see all detailed findings"
  artifacts:
    - path: "src/components/LoadingAnimation.jsx"
      provides: "Playful loading animation with rotating encouraging messages"
      min_lines: 50
    - path: "src/components/ReportCard.jsx"
      provides: "Progressive disclosure toggle (minimal → expanded findings)"
      contains: "showAllFindings"
    - path: "tests/ui/loading-animation.spec.js"
      provides: "Component test for loading animation and messages"
      exports: ["test"]
    - path: "tests/ui/report-card-interactions.spec.js"
      provides: "Component test for report card progressive disclosure"
      exports: ["test"]
  key_links:
    - from: "src/components/ReviewPanel.jsx"
      to: "src/components/LoadingAnimation.jsx"
      via: "phase === 'loading' renders LoadingAnimation"
      pattern: "phase === 'loading'.*LoadingAnimation"
    - from: "src/components/ReportCard.jsx"
      to: "useState(false)"
      via: "showAllFindings state toggle"
      pattern: "useState.*showAllFindings"
---

<objective>
Add playful loading animation with encouraging messages and progressive disclosure to report card display.

Purpose: Fulfill REVW-08 (friendly loading state) and REVW-07 (color-coded grades with minimal default view).
Output: LoadingAnimation component, progressive disclosure in ReportCard, component tests
</objective>

<execution_context>
@/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-report-card-ui/03-CONTEXT.md
@.planning/phases/03-report-card-ui/03-RESEARCH.md
</context>

<interfaces>
<!-- Existing component interfaces executor needs -->

From src/components/ReviewPanel.jsx (phase state machine):
```jsx
const [phase, setPhase] = useState('input'); // 'input' | 'loading' | 'report' | 'fallback' | 'deep-dive'
```

From src/components/ReportCard.jsx (existing structure):
```jsx
export default function ReportCard({ data, filename, onDeepDive, onNewReview })
// data shape: { overallGrade, topPriority, categories: { bugs, security, readability, completeness } }
```

Tailwind animation utilities (per RESEARCH.md):
```css
animate-bounce  /* Built-in bouncing animation */
transition-opacity duration-300  /* Fade transitions */
```

React hooks for implementation:
```jsx
import { useState, useEffect } from 'react';
```

Lucide React icons (replace emoji):
```jsx
import { ChevronDown, ChevronUp } from 'lucide-react';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Create component test scaffolds for loading and progressive disclosure</name>
  <files>tests/ui/loading-animation.spec.js, tests/ui/report-card-interactions.spec.js</files>
  <action>
Create two Playwright component test files:

**tests/ui/loading-animation.spec.js:**
- Test: LoadingAnimation displays bouncing dots animation
- Test: Encouraging messages rotate during loading (check for multiple messages)
- Test: aria-live region announces loading state for screen readers
- Test: Component displays filename if provided
- Use Playwright component testing API (@playwright/experimental-ct-react)

**tests/ui/report-card-interactions.spec.js:**
- Test: Report card displays color-coded grades with icons/labels (not color alone)
- Test: Default view shows minimal layout (overall grade, top priority, grade summary)
- Test: "Show all findings" toggle expands to reveal detailed findings
- Test: Toggle collapses findings back to minimal view
- Use component test with mock data prop (sample report card JSON)

Both tests should fail initially (components not yet updated). Follow Playwright component testing best practices: mount(), locator, expect assertions.
  </action>
  <verify>
    <automated>npm run test:ui</automated>
  </verify>
  <done>Test files exist at correct paths (tests/ui/*), tests fail initially (expected), run without syntax errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Create LoadingAnimation component with encouraging messages</name>
  <files>src/components/LoadingAnimation.jsx, src/components/ReviewPanel.jsx</files>
  <behavior>
    - LoadingAnimation displays bouncing dots animation (Tailwind animate-bounce)
    - Messages rotate every 3-4 seconds from array of 4 encouraging phrases
    - Messages match friendly-teacher tone from CONTEXT.md:
      * "Looking for ways to make your code even better!"
      * "Checking for any gotchas..."
      * "Making sure everything's ship-shape!"
      * "Scanning for those sneaky edge cases..."
    - Component includes aria-live region for screen reader accessibility
    - Component displays filename if provided
    - Component shows realistic timing expectation (30-120 seconds)
  </behavior>
  <action>
Create src/components/LoadingAnimation.jsx:

**Implementation details:**
- Accept `filename` prop (optional)
- Use useState for message rotation index
- Use useEffect with setInterval (3500ms) to rotate messages
- Clean up interval on unmount
- Bouncing dots: 3 dots with staggered animationDelay (0ms, 150ms, 300ms) using Tailwind animate-bounce
- aria-live="polite" region for accessibility (always in DOM, content updates)
- Layout: centered flex container, vertical spacing, subtle transitions
- Styling: Use indigo color palette to match app theme, text-slate-300 for messages

**Integrate into ReviewPanel.jsx:**
- Import LoadingAnimation at top
- In render, when `phase === 'loading'`, render LoadingAnimation component instead of generic loading message
- Pass `filename` prop if available
- Replace existing loading UI (likely around line 100-120)

**Run tests to verify:** Tests in loading-animation.spec.js should pass after implementation.
  </action>
  <verify>
    <automated>npm run test:ui</automated>
  </verify>
  <done>LoadingAnimation component renders with rotating messages, bouncing animation, and aria-live accessibility. ReviewPanel uses new component. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add progressive disclosure toggle to ReportCard</name>
  <files>src/components/ReportCard.jsx</files>
  <behavior>
    - Default view: minimal (overall grade, top priority, grade summary grid with finding counts)
    - "Show all findings" button with chevron icon appears below grade summary
    - Clicking button expands to show all FindingCard components per category
    - Button label and icon change when expanded ("Hide detailed findings" + ChevronUp)
    - Expanded findings use existing FindingCard component (already in ReportCard.jsx)
  </behavior>
  <action>
Update src/components/ReportCard.jsx:

**Add progressive disclosure:**
1. Import useState, ChevronDown, ChevronUp from lucide-react
2. Add state: `const [showAllFindings, setShowAllFindings] = useState(false);`
3. Restructure render to always show: OverallGradeHeader, TopPriorityCallout, GradeSummaryGrid
4. Add toggle button below grade summary:
   - Label: "Show all findings" (default) / "Hide detailed findings" (expanded)
   - Icon: ChevronDown (default) / ChevronUp (expanded)
   - Styling: flex items-center gap-2, border, rounded-lg, hover states
5. Conditionally render detailed findings section when `showAllFindings === true`
   - Wrap in `{showAllFindings && (<div>...</div>)}`
   - Use existing CategorySection components with FindingCard rendering

**DO NOT replace emoji icons in this task** — emoji icon replacement is handled by Plan 03-02 Task 3 to avoid file conflicts in ReportCard.jsx. That task will replace CATEGORY_LABELS emoji with Lucide React SVG icons (Bug, Lock, BookOpen, CheckCircle).

**Preserve existing functionality:**
- Keep all existing props (data, filename, onDeepDive, onNewReview)
- Keep existing color-coded grades (GRADE_COLORS mapping)
- Keep existing FindingCard component with expand/collapse
- Keep existing GradeBadge, SeverityPill components
- Keep existing CATEGORY_LABELS unchanged (Plan 02 will update icons)

**Run tests to verify:** Tests in report-card-interactions.spec.js should pass after implementation.
  </action>
  <verify>
    <automated>npm run test:ui</automated>
  </verify>
  <done>ReportCard displays minimal view by default with "Show all findings" toggle. Clicking expands to show all findings. Tests pass.</done>
</task>

</tasks>

<verification>
Run full component test suite for Phase 03 Plan 01:
```bash
npm run test:ui
```

Manual verification:
1. Start dev server: `npm run dev`
2. Navigate to Review mode
3. Submit code for review
4. Confirm loading animation appears with rotating encouraging messages
5. Confirm report card displays minimal view by default
6. Confirm "Show all findings" button expands to show detailed findings
</verification>

<success_criteria>
- [x] LoadingAnimation component created with 4 rotating encouraging messages
- [x] Bouncing dots animation uses Tailwind animate-bounce
- [x] aria-live region included for screen reader accessibility
- [x] ReviewPanel renders LoadingAnimation when phase === 'loading'
- [x] ReportCard has progressive disclosure toggle
- [x] Default view shows minimal layout (overall grade, top priority, grade summary)
- [x] Expanded view shows all FindingCard components
- [x] Component tests pass for loading animation and progressive disclosure
</success_criteria>

<output>
After completion, create `.planning/phases/03-report-card-ui/03-01-SUMMARY.md`
</output>
