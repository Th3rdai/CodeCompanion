# Phase 24.5-02 — Summary

## Done

- Added `src/hooks/useModels.js` — models list, connection, `ollamaUrl`, persisted `selectedModel`, `autoResolvedLabel`, `isVisionModel` (boolean for current selection), `refreshModels`, `refreshing`.
- Added `src/hooks/useChat.js` — chat/history state, SSE `handleSend`, history CRUD, `startNew`, `handleStopChat`, `handleSaveChat`, Escape + auto-send `useEffect`s, `pendingAutoSend` ref.
- Added `src/hooks/useImageAttachments.js` — image/document attach pipeline (privacy warning, queueing, upload/drop/paste handlers, lightbox state/navigation).
- `App.jsx` composes hooks after `mode` + `showOllamaSetup`; restores `handleSaveReview` / builder / pentest handlers that were incorrectly removed during splice.
- Mount + settings save call `refreshModels()` instead of inlined `fetchModels`.
- Exposes `setActiveConvId` from `useChat` for review/pentest/builder save paths.
- Removed dead code: duplicate `handleSaveChat`, unused `handleExportOffice` (export lives in `ExportPanel`), unused imports / `TypingIndicator` helper; extracted image logic from `App.jsx`.

## Verify

- `npm run build` — pass  
- `node --test tests/unit/*.test.js` — 163 pass  
- ESLint on touched files — 0 errors (exhaustive-deps warnings only, pre-existing pattern for mount effect)
- `wc -l src/App.jsx` — **1873** lines (target met: under 2000)

## Follow-up

- Phase 24.5-02 targets are complete. Remaining frontend cleanup can be scheduled separately (optional dependency warning cleanup / further panel-specific hook extraction).
