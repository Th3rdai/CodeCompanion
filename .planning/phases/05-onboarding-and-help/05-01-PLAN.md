---
phase: 05-onboarding-and-help
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/OnboardingWizard.jsx
autonomous: true
requirements: [UX-01, UX-04]

must_haves:
  truths:
    - "First-time user sees onboarding wizard on launch explaining Code Companion for vibe coders"
    - "Onboarding uses friendly-teacher tone with analogies and zero jargon"
    - "Ollama setup step includes troubleshooting guidance for non-technical users"
    - "Mode descriptions match vibe-coder audience (not PM-focused)"
    - "Privacy messaging appears in onboarding step 4 with clear local-only assurance"
  artifacts:
    - path: "src/components/OnboardingWizard.jsx"
      provides: "4-step wizard with vibe-coder content and Lucide icons for modes"
      min_lines: 250
  key_links:
    - from: "App.jsx"
      to: "OnboardingWizard.jsx"
      via: "isOnboardingComplete() check"
      pattern: "isOnboardingComplete"
    - from: "OnboardingWizard STEPS array"
      to: "localStorage th3rdai_onboarding_complete"
      via: "finish() persistence"
      pattern: "localStorage\\.setItem.*onboarding_complete"
---

<objective>
Transform existing OnboardingWizard from PM-focused to vibe-coder audience, matching Phase 2's friendly-teacher tone with analogies and zero jargon. Add Ollama troubleshooting guidance for non-technical users and replace mode emoji icons with Lucide React icons.

Purpose: Align onboarding with Phase 2's vibe-coder tone shift, help first-time users set up Ollama successfully, and meet UX-01 (onboarding flow) and UX-04 (privacy messaging) requirements.

Output: Updated OnboardingWizard.jsx with vibe-coder content, Ollama troubleshooting, and Lucide mode icons.
</objective>

<execution_context>
@/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/james/AI_Dev/AIApp-CodeCompanion/.planning/PROJECT.md
@/Users/james/AI_Dev/AIApp-CodeCompanion/.planning/ROADMAP.md
@/Users/james/AI_Dev/AIApp-CodeCompanion/.planning/STATE.md
@/Users/james/AI_Dev/AIApp-CodeCompanion/.planning/phases/05-onboarding-and-help/05-CONTEXT.md
@/Users/james/AI_Dev/AIApp-CodeCompanion/.planning/phases/05-onboarding-and-help/05-RESEARCH.md

# Relevant prior work
@/Users/james/AI_Dev/AIApp-CodeCompanion/.planning/phases/02-tone-unification/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Key existing interfaces and patterns the executor needs -->

From .planning/phases/02-tone-unification/02-01-SUMMARY.md:
- Mode-specific personalities: explain=patient teacher, bugs=protective friend, refactor=helpful coach
- Inline jargon definitions pattern: plain-English explanations, not technical terms
- "AI coding tool" references (not "dev team" or "engineering team")
- Analogies from everyday life (library, kitchen, recipe, closet)

From src/components/OnboardingWizard.jsx (existing):
```typescript
// Component exports
export function isOnboardingComplete(): boolean;
export function resetOnboarding(): void;
export default function OnboardingWizard({ onComplete }: { onComplete: () => void }): JSX.Element;

// Storage key (DO NOT CHANGE)
const STORAGE_KEY = 'th3rdai_onboarding_complete';

// STEPS structure
const STEPS = [
  { id: string, title: string, subtitle: string, content: JSX.Element, icon: string },
  // ... 4 steps total
];
```

From lucide-react (already installed 0.577.0):
```typescript
import { MessageCircle, Lightbulb, ArrowRightLeft, Bug, Sparkles, FileCheck, WrenchIcon, Hammer } from 'lucide-react';
// Use <Icon className="w-4 h-4" /> for mode grid icons
```

From .claude/skills/ui-ux-pro-max/SKILL.md:
- Rule: Use SVG icons (Lucide), not emojis for UI elements
- Exception granted by user: Step indicator emoji (👋, 🔌, 🎯, 🛡️) are OK for friendly tone
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite onboarding content for vibe-coder audience with Ollama troubleshooting and Lucide icons</name>
  <files>src/components/OnboardingWizard.jsx</files>
  <action>
Update all 4 STEPS in OnboardingWizard.jsx to match vibe-coder tone and user decisions from 05-CONTEXT.md:

**Step 1 (Welcome) - Lines 5-22:**
- Replace "helps Product Managers" (line 13-14) with "translates AI-generated code into honest, plain-English reviews — so you know what's safe to ship and what needs fixing"
- Keep subtitle "Your friendly guide to all things code"
- Add framing: "Whether you're using Cursor, ChatGPT, or any AI coding tool, Code Companion helps you understand what your code actually does and where the risks are"
- Keep privacy line (line 17-18) as-is
- Keep icon: '👋' (step indicator emoji exception)

**Step 2 (Connect to Ollama) - Lines 23-44:**
- Keep title and subtitle
- Keep existing setup instructions (lines 29-40)
- ADD after line 40, before closing fragment: Troubleshooting guidance per 05-RESEARCH.md Pattern 2:
  ```jsx
  <div className="glass rounded-lg p-3 text-xs text-slate-400 space-y-1.5 mt-3">
    <p><strong className="text-slate-300">Troubleshooting:</strong></p>
    <ul className="list-disc list-inside space-y-1 ml-2">
      <li><strong>Port not responding?</strong> Make sure Ollama is running (check for the icon in your menu bar)</li>
      <li><strong>No models installed?</strong> Run <code className="bg-slate-700/50 px-1.5 py-0.5 rounded text-indigo-300">ollama list</code> to check, then <code className="bg-slate-700/50 px-1.5 py-0.5 rounded text-indigo-300">ollama pull llama3.2</code> to download</li>
      <li><strong>Connection refused?</strong> Check Settings to verify the Ollama URL matches your setup (default: <code className="bg-slate-700/50 px-1.5 py-0.5 rounded text-indigo-300">http://localhost:11434</code>)</li>
    </ul>
  </div>
  ```
- Keep icon: '🔌'

**Step 3 (Pick Your Mode) - Lines 45-77:**
- Change subtitle from "Eight specialized tools for your workflow" to "Eight ways to understand and improve your code"
- Change intro text (line 51-52) from "Each mode is tuned for a specific PM task" to "Each mode helps with a different part of building with AI"
- REPLACE emoji icons with Lucide icons per 05-CONTEXT.md Decision 4:
  - Add import at top: `import { MessageCircle, Lightbulb, ArrowRightLeft, Bug, Sparkles, FileCheck, WrenchIcon, Hammer } from 'lucide-react';`
  - Change mode array (lines 55-63):
    * { icon: MessageCircle, label: 'Chat', desc: 'Ask anything about your code' }
    * { icon: Lightbulb, label: 'Explain', desc: 'Translate code into plain English' }
    * { icon: ArrowRightLeft, label: 'Tech to Biz', desc: 'Explain your code to anyone' }
    * { icon: WrenchIcon, label: 'Biz to Tech', desc: 'Turn ideas into AI prompts' }
    * { icon: Bug, label: 'Safety Check', desc: 'Find what could break' }
    * { icon: FileCheck, label: 'Review', desc: 'Get an honest code report card' }
    * { icon: Sparkles, label: 'Clean Up', desc: 'Make your code better' }
    * { icon: Hammer, label: 'Create', desc: 'Start new projects fast' }
  - Update grid item rendering (lines 64-72) to render icon component instead of emoji:
    ```jsx
    <div key={m.label} className="glass rounded-lg p-2 flex items-center gap-2">
      <m.icon className="w-4 h-4 text-indigo-400" />
      <div>
        <span className="text-slate-200 font-medium">{m.label}</span>
        <span className="text-slate-500 ml-1">{m.desc}</span>
      </div>
    </div>
    ```
- Keep icon: '🎯'

**Step 4 (Privacy) - Lines 78-104:**
- Keep title, subtitle, and privacy points structure
- Verify messaging matches UX-04: "Your code never leaves your computer" sentiment is clear
- Content is already good (local storage, no tracking, no API keys) — no changes needed
- Keep icon: '🛡️'

**DO NOT change:**
- STORAGE_KEY constant (line 3) — breaks user state persistence
- isOnboardingComplete() function (lines 106-112)
- resetOnboarding() function (lines 114-118)
- Component structure, keyboard navigation (lines 158-162), or state management

**Pattern to follow:**
- Use analogies from everyday life (like Phase 2: library, kitchen, recipe)
- Zero jargon — if technical term used, explain inline
- Friendly-teacher tone — patient, encouraging, never condescending
- "You" perspective (not "users" or "developers")
  </action>
  <verify>
    <automated>npm run test:ui -- OnboardingWizard.spec.jsx 2>/dev/null || echo "MISSING — Wave 0 must create tests/ui/OnboardingWizard.spec.jsx first"</automated>
  </verify>
  <done>OnboardingWizard.jsx updated with vibe-coder content, Ollama troubleshooting (3 bullets), Lucide mode icons, and privacy messaging intact. Step 1 references "AI coding tool" instead of "Product Managers". Mode descriptions simplified for first-time users. localStorage keys unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify onboarding localStorage persistence and keyboard navigation</name>
  <files>src/components/OnboardingWizard.jsx</files>
  <action>
Manual verification of onboarding behavior (automated test not yet created — Wave 0 gap):

1. **First launch test:**
   - Clear localStorage: Open DevTools Console, run `localStorage.removeItem('th3rdai_onboarding_complete')`
   - Refresh browser
   - Verify OnboardingWizard displays automatically
   - Verify all 4 steps display correct vibe-coder content (Step 1 says "AI coding tool", Step 3 shows Lucide icons not emoji)

2. **Completion persistence test:**
   - Click through all 4 steps to "Let's Go!"
   - Verify wizard closes
   - Refresh browser
   - Verify wizard does NOT reappear (localStorage persisted)

3. **Keyboard navigation test:**
   - Reopen wizard: `localStorage.removeItem('th3rdai_onboarding_complete')` and refresh
   - Tab to focus wizard, press ArrowRight → step advances
   - Press ArrowLeft → step goes back
   - Press Escape → wizard closes
   - Verify keyboard navigation works without mouse

4. **Ollama troubleshooting visibility test:**
   - Navigate to Step 2 (Connect to Ollama)
   - Verify troubleshooting section appears below setup instructions
   - Verify 3 troubleshooting bullets visible with code snippets styled correctly

5. **Icon consistency test:**
   - Navigate to Step 3 (Pick Your Mode)
   - Verify mode grid shows Lucide SVG icons (not emoji) for all 8 modes
   - Verify step indicators still use emoji (👋, 🔌, 🎯, 🛡️)

Document any issues in task output. If all checks pass, mark task complete.
  </action>
  <verify>
    <automated>echo "Manual verification complete — see task output for results"</automated>
  </verify>
  <done>Onboarding wizard displays on first launch, persists completion to localStorage, supports keyboard navigation (arrow keys, Enter, Escape), shows Ollama troubleshooting in Step 2, uses Lucide icons for mode grid while preserving emoji for step indicators, and does not reappear after completion.</done>
</task>

</tasks>

<verification>
**Overall phase checks:**
1. `npm run dev` → visit http://localhost:5173 → clear localStorage → wizard appears
2. Wizard Step 1 content references "AI coding tool" and "vibe coders", not "Product Managers"
3. Wizard Step 2 includes troubleshooting section with 3 bullets after setup instructions
4. Wizard Step 3 mode grid uses Lucide icons (SVG), not emoji
5. Wizard Step 4 privacy messaging clearly states "Your code never leaves your computer"
6. Complete wizard → refresh → wizard does not reappear (localStorage works)
7. Keyboard navigation: ArrowRight/Left, Enter, Escape all work
8. `grep "Product Managers" src/components/OnboardingWizard.jsx` returns no matches
</verification>

<success_criteria>
- [ ] OnboardingWizard.jsx Step 1 updated to vibe-coder framing ("AI coding tool" not "Product Managers")
- [ ] OnboardingWizard.jsx Step 2 includes 3-bullet troubleshooting section below setup instructions
- [ ] OnboardingWizard.jsx Step 3 mode grid uses Lucide React icons (MessageCircle, Bug, etc.)
- [ ] Step indicator emoji (👋, 🔌, 🎯, 🛡️) preserved as exception
- [ ] localStorage key 'th3rdai_onboarding_complete' unchanged (persistence works)
- [ ] Keyboard navigation (arrow keys, Escape) still functional
- [ ] UX-01 requirement addressed: First-time onboarding flow with vibe-coder tone
- [ ] UX-04 requirement addressed: Privacy messaging visible in Step 4
</success_criteria>

<output>
After completion, create `.planning/phases/05-onboarding-and-help/05-01-SUMMARY.md`
</output>
