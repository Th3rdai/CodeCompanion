# 2026-05-08

22:25 UTC | Pass | E:0 W:0 | P1:OK P2:OK P3:OK P4:OK P5:OK P6:OK P7:OK P8:OK | Full `/validate-project`: P1 Prettier `--write` on `journal/README.md` then clean; P2 vite **6.85s**, mermaid chunk **mermaid-DisNUZHa.js**; P3/P7 **HTTP** `:8903` (`FORCE_HTTP=1`); P4 **640** unit; MCP stdio **19**; P5 UI **35**/15 skip **0** flaky; P6 E2E **23**; P7 scoped `files/read`+repo `files/save`+backup, traversals **403**; P8 `validate:p7` vs `:8903` — **llama3.2:latest**, chat/review/diagram SSE, **19** MCP tools, 12 prompts.

21:33 UTC | Pass | E:0 W:1 | P1:OK P2:OK P3:partial P4:OK P5:OK P6:OK P7:partial P8:skip | `/validate-project` resume: onboarding Playwright specs + `aria-label` restored to **Welcome wizard**; UI **34** pass / **15** skip / **1** flaky (report-card `beforeEach` timeout, retry pass); E2E **23**; P8 not run.

19:55 UTC | Pass | E:0 W:0 | P1:OK P2:OK P3:OK P4:OK P5:skip P6:skip P7:OK P8:OK | `/validate-project --thorough` after CTXFIX Phase 4 (verify) + Phase 5 (docs) + Phase 1a (`/api/model-context`) + Phase 3 (tool-result externalization). P1: prettier auto-fix on 28 files (mostly new CTXFIX files), then clean. P2: vite build 6.58s, mermaid chunk separate (644 KB). P3: HTTPS on `:8903`. P4: **618** unit / **0** fail; MCP stdio: **19** tools. P7: config + models (25) + history CRUD + file tree + path-traversal blocked (403); Phase 1a route returns documented JSON shape (`cloud-hint` 128 000, unknown→null, auto resolves `kimi-k2:1t-cloud`, missing query→400); MCP HTTP initialize OK. P8: `validate:p7` passed — chat SSE / review JSON report card / diagram SSE / 19 MCP tools / all 12 mode prompts. P5/P6 (Playwright UI+E2E) skipped — covered by recent runs and not the focus of this CTXFIX validation. Phase 2 (history compaction) intentionally NOT shipped this round (HIGH risk, scheduled for v1.7.2+).

16:35 UTC | Pass | Integration only | `npm run test:integration` — **22** pass / **0** fail / **0** skip (~68 s). Suites: `api-with-images`, `review-folder`, `experiment-api`, `history-folders-api`, `mcp-connect-failure`, `parallel-tools`. Ollama required for image + review + folder-review LLM paths.

23:27 | Fail | E:0 W:2 | P1:FAIL P2:OK P3:OK P4:OK P5:OK P6:OK P7:FAIL P8:OK | `/validate-project --thorough`: P1 failed strict `prettier --check` (9 files, mostly pre-existing docs/tests). P2 vite build OK + mermaid chunk. P3 HTTPS `:8903` + Ollama reachable. P4 **684** unit pass, MCP stdio **19** tools. P5 UI **35** pass / **15** skip. P6 E2E **23** pass (after clearing `:4173` conflict). P7 strict command-path checks failed under current root policy (`/api/files/read?path=package.json` -> Not found; `/api/files/save` to `/tmp` -> outside allowed dirs), while other API checks and traversal 403 guards passed. P8 `validate:p7` passed (`llama3.2:latest`).
