## MCP Per-Tool Enable/Disable — Implemented & Verified

### Work completed

**MCPFIX gaps closed (both gaps from MCPFIX.md):**

- **Gap 1 — ToolsModal UX** (`src/components/McpClientPanel.jsx`):
  - Added `SERVICE_LABELS` map + `getGroup()` at module level for Google Workspace `{verb}_{service}_{noun}` naming; includes plural keys (`calendars`, `tasks`, `contacts`, `forms`).
  - Replaced flat 121-item list with grouped, collapsible sections (Gmail, Drive, Docs, Calendar, Sheets, Chat, Forms, Slides, Tasks, Contacts, Apps Script, Search, Google Auth).
  - Real-time search input filters by name + description; auto-expands collapsed groups while typing.
  - Group-level checkbox enables/disables all tools in a group atomically.
  - Modal widened from `max-w-lg` → `max-w-2xl`.
  - `useGroups` flag: flat list for ≤10 tools, grouped for larger servers — no regression for small MCP clients.
  - `enabledCount` now computed against visible tool names only (prevents stale-name undercount).
  - Enable all / Disable all footer buttons unchanged; apply to full tools array.

- **Gap 2 — Execution-time denial** (`lib/tool-call-handler.js`):
  - Added guard before `callTool`: reads `config.mcpClients`, finds matching client by `serverId`, returns `{ success: false, error: "...disabled..." }` if `toolName` in `disabledTools`.
  - Logs WARN with `serverId` + `toolName` only (no args logged).
  - 3 new unit tests added; all **225 tests pass**.

- **Route hardening** (`lib/mcp-api-routes.js` + `lib/mcp-client-manager.js`):
  - `validateAndNormalizeConfig()` now trims, de-duplicates, and type-checks `disabledTools` entries.
  - `PUT /api/mcp/clients/:id` validates before disconnecting; detects `disabledTools`-only updates and saves without interrupting the active MCP connection.

**Build + install:**

- Frontend rebuilt (`npm run build`), Electron Mac app built (`npm run electron:build:mac`), installed to `/Applications/Code Companion.app`.

### Verified working in production

Log evidence from 17:22 session:

- `PUT /mcp/clients/google 200 0ms` — save completes instantly, no reconnect (route hardening confirmed).
- `toolsLength: 28417` vs prior `30120` — disabled tools are filtered from model prompt.
- All 5 MCP clients auto-connected: Archon (17), Google (121), crawl4ai-rag (5), Stitch (12), nano banana (4).
- Zero WARN/ERROR entries in current session.

### Known pending

- Model capability note: `qwen3-coder:30b` does not reliably emit `TOOL_CALL` format for complex Google MCP flows — model hallucinated OAuth URLs instead of calling tools. Use `bazobehram/qwen3-14b-claude-4.5-opus-high-reasoning` or stronger for Google Workspace agentic tasks.

---

## 13:37 | Pass | E:0 W:6 | P1:OK P2:OK P3:OK P4:OK P5:OK P6:OK P7:OK P8:OK

Image generation pipeline fully fixed and validated. 231 unit tests, UI 32/15 skip, E2E 22/22, P7 all endpoints clean, P8 workflow smoke passed.

**Fixes shipped this session:**

- `routes/chat.js`: strip `<think>` / `<thought>` reasoning blocks from finalText before streaming; improved `[IMAGE_DELIVERED]` model instruction; `terminalCmd` SSE progress event for image tools
- `lib/mcp-client-manager.js` + `lib/tool-call-handler.js`: MCP SDK 60s internal timeout bypassed — now passes `MCP_IMAGE_TOOL_TIMEOUT_MS` (180s) directly to `conn.client.callTool()` options
- `~/Library/Application Support/code-companion/.env`: updated `GEMINI_API_KEY` to correct paid Tier 1 key (old key was free-tier from different project)
- `lib/auto-model.js`: `chat` mode default changed from `kimi-k2:1t-cloud` → `gemma4`; `gemma4` added as first entry in `TOOL_CALL_CAPABLE` (confirmed reliable tool-call emitter, 15s image gen)
- Prettier formatted 15 files (P1 now clean)

---

## 21:27 | Pass | E:0 W:0 | P1:OK P2:OK P3:OK P4:OK P5:OK P6:OK P7:OK P8:OK

ImageLightbox zoom/pan fix and full validation pass.

**Fix shipped this session:**

- `src/components/ImageLightbox.jsx`: drag/pan handlers moved to full overlay div (were on `<img>` — clipped when zoomed); `pointerEvents:none` on img; `translate` before `scale` in transform so pan works in screen space; auto-reset position on zoom-out to 1×; cursor shows grab/grabbing state; hint updated to "Drag to pan • scroll to zoom"
- Electron app rebuilt (`npm run electron:build:mac`) and installed to `/Applications/Code Companion.app`

**Test counts:** 236 unit, 32 UI / 15 skip, 22 E2E, P7 all endpoints, P8 warm+chat+review+diagram SSE OK, MCP 17 tools, 12 mode prompts
