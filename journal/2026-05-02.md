# 2026-05-02 — Post-v1.6.33 cleanup + smoke test

Day-after follow-ups to yesterday's v1.6.24 → v1.6.33 ship cycle. No new release tag — seven commits land on master ahead of v1.6.33; will bundle into v1.6.34 whenever that's cut.

## Commits (master ahead of v1.6.33)

| sha       | what                                                                                                                                                                                                                                                                                                                                                      |
| --------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `4f07c69` | `chore(mcp)`: Archon auto-connect failure downgraded ERROR → WARN with hint to set `autoConnect: false`                                                                                                                                                                                                                                                   |
| `50d0d00` | `fix(mcp)`: manual-connect failures now surface real transport error in banner + app.log. Root cause was `logMcpConnectFailure()` declared at module top-level referencing `log` from a non-existent scope — every call threw silent `ReferenceError`, Express converted to generic 500. Moved into `createMcpApiRoutes` closure.                         |
| `c60e3c3` | (Cursor) Prompt: prefer Crawl4AI `search_web` over Playwright for web search                                                                                                                                                                                                                                                                              |
| `cc2c247` | `fix(useChat)`: LinkedExperimentChips reads legacy singular `experimentId` as fallback when plural `experimentIds` is absent. v1.6.23 → v1.6.24 schema migration didn't backfill on read; user's existing experiment-mode chats had the singular field.                                                                                                   |
| `87401cf` | `fix(experiment)`: new `POST /api/experiment/:id/link-conversation` + ExperimentPanel call after `saveHistory` returns convId. Closes the order-of-operations gap where experiment was created BEFORE the chat existed, leaving `conversationId: null` on the experiment record (chip-restore lost chat context). Idempotent; fire-and-forget on failure. |
| `08728bb` | `chore`: prettier on 4 files Cursor committed unformatted (`CLAUDE.md`, `docs/CRAWL4AI-RAG-MCP.md`, `lib/resolve-mcp-test-config-root.js`, `scripts/test-mcp-clients.js`). Added `e2e-screenshots/` + `e2e-test-report.md` to `.gitignore` so Playwright artifacts stop polluting the tree.                                                               |
| `0d73e3f` | (Cursor) MCP smoke-test config resolution + Crawl4AI docs + Settings log hints                                                                                                                                                                                                                                                                            |

## Smoke test against installed app v1.6.33

Spawned 4 parallel sub-agents against `http://127.0.0.1:8910`:

- **Smoke A (Experiment lifecycle)**: PASS 9/9. duplicate-start 409, trust boundary, abort, migration shim — all working. Server correctly rejected client-supplied `done:true`/`decision:keep` and used its own parser output (`done:false`, `decision:iterate`).
- **Smoke B (Review/Score/Pentest)**: PASS 5/5. Both Review (grade A in 13.7s) and Pentest (grade F in 24s) returned structured JSON, no SSE fallback. Error envelopes match v1.6.33 wording.
- **Smoke C (History → experimentIds chip)**: PARTIAL. Endpoint plumbing correct (`?include=experiments` hydrates), but 0 of 29 conversations had `experimentIds` populated — all 2 experiment-mode chats use legacy singular `experimentId`. Surfaced two real bugs (legacy fallback + conversationId backfill) — both fixed in `cc2c247` + `87401cf`.
- **Smoke D (app.log scan)**: CLEAN. 0 real code issues. 11 ERROR+WARN lines today, all categorized as known/expected/external. 36 tool-call rounds. Duplicate-experiment guard fired once (working as designed).

## Archon status

Connection to `http://192.168.50.7:8051/mcp` works (handshake + tool list), but Archon's upstream API service is degraded. `health_check` returns:

```json
{ "status": "degraded", "api_service": false, "agents_service": false }
```

`find_projects` returns connection_error. Task management via Archon MCP is unavailable until the upstream service is back. Doc updates captured locally instead.

## Validation at end of day

- `validate:fast` clean (lint + typecheck + format + vite + 403 unit + 8 integration + smoke)
- working tree empty
- master 7 commits ahead of v1.6.33 (5 mine, 2 Cursor's)
- installed app v1.6.33; auto-updater will offer v1.6.34 once tagged

## Lessons

- **Smoke-test sub-agents in parallel** worked well. Four endpoint domains × four sub-agents × ~1 min each, vs. ~4 min sequential. Each report was concise enough to surface real findings (the singular-`experimentId` legacy schema would not have been obvious from code review alone).
- **Error-handler bugs are silent** because the error path itself rarely runs in tests. The `logMcpConnectFailure` ReferenceError lurked for at least a few releases, only surfacing when MCP servers actually failed and the user clicked Connect. Worth scanning for similar shapes: top-level functions in modules that reference closure variables.
- **Schema migrations on read are cheap insurance.** v1.6.24's `_migrate()` shim covered the experiment record migration but the corresponding `experimentId` → `experimentIds` migration on the chat-history side was missed. A pattern to consider when shipping schema changes: read-side fallback for at least one prior shape.
