# 2026-04-03

23:00 | Pass | E:0 W:1 | P1:OK P3:OK P4:OK P5:OK | **`/validate-project`** (default): `vite build` OK; **`npm run test:unit`** **148** pass; Playwright **`tests/ui/` + `tests/e2e/`** (webServer `FORCE_HTTP=1`) **48** pass / **15** skip / **1** flaky (`report-card-interactions` `#model-select` visibility — passed on retry). Manual HTTPS `:4173` smoke not run (env); P6/P7 API not re-run this pass.

22:42 | Pass | E:0 W:1 | P1:OK P2:OK P3:OK P4:OK P5:OK P6:OK P7:OK | **`/validate-project --thorough`** (retry): Vite + mermaid chunk; HTTPS `:4173` + `PW_REUSE_SERVER=1` `BASE_URL=https://127.0.0.1:4173`; **146** unit + MCP/tone/labels/rate-limit; UI **32** pass / 15 skip; E2E **17/17** (duplicate-upload fix: `waitForEvent('dialog')`); P6 file save + backup under **project** `folder`; MCP **11** tools; P7 chat SSE `data:`, 12 mode prompts; Vite CSS warning only.

15:45 | Fail | E:1 W:2 | P1:OK P2:OK P3:OK P4:OK P5:FAIL P6:OK P7:OK | `/validate-project --thorough`: Vite build + mermaid chunk OK; HTTPS `:4173`; 146 unit + MCP/tone/labels/rate-limit; Playwright UI 32 pass / 15 skip; E2E 16 pass / 1 fail (`image-upload.spec.js` duplicate uploads / `dialog.dismiss: Test ended`); P6 API/history/MCP/GitHub/files tree + path 403; file save to `/tmp` rejected (folder outside allowed dirs — use project `folder`); P7 review SSE OK, chat `data:` seen, MCP tools/list **11**, 12 mode prompts OK; vite CSS `file:line` warning.
